[railway]
environment = "production"

[deploy]
startCommand = "node backend/src/migrations/run.js && node backend/src/migrations/seed.js && node backend/src/server.js"
healthCheckPath = "/api/products"

[[services]]
name = "backend"
rootDirectory = "."
