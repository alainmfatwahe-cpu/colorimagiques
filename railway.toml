[railway]
environment = 'production'

[build]
builder = 'nixpacks'

[deploy]
startCommand = 'node backend/src/server.js'
healthCheckPath = '/api/products'

[[services]]
name = 'backend'
rootDirectory = '.'
