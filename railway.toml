[railway]
environment = "production"

[build]
builder = "docker"
dockerfile = "Dockerfile"

[deploy]
startCommand = "node backend/src/server.js & nginx -g 'daemon off;'"
healthCheckPath = "/api/products"

[[services]]
name = "backend"
rootDirectory = "."
