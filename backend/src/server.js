// backend/src/server.js// Point d'entrÃ©e principal â€” Serveur Express ColoriMagiquesimport express from 'express';import cors from 'cors';import helmet from 'helmet';import rateLimit from 'express-rate-limit';import morgan from 'morgan';import path from 'path';import { fileURLToPath } from 'url';import dotenv from 'dotenv';dotenv.config();

const __filename = fileURLToPath(import.meta.url);const __dirname = path.dirname(__filename);// Import routesimport authRoutes from './routes/auth.js';import productRoutes from './routes/products.js';import orderRoutes from './routes/orders.js';import downloadRoutes from './routes/downloads.js';import reviewRoutes from './routes/reviews.js';import seoRoutes from './routes/seo.js';import newsletterRoutes from './routes/newsletter.js';const app = express();
app.set('trust proxy', 1);const PORT = process.env.PORT || 4000;const FRONTEND_URL = process.env.FRONTEND_URL || 'http://localhost:5173';// â”€â”€â”€ SÃ©curitÃ© â”€â”€â”€app.use(helmet({  crossOriginResourcePolicy: { policy: 'cross-origin' },  contentSecurityPolicy: false, // DÃ©sactivÃ© pour permettre les CDN frontend}));// â”€â”€â”€ CORS â”€â”€â”€app.use(cors({  origin: [FRONTEND_URL, 'http://localhost:3000', 'http://localhost:5173'],  credentials: true,  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS'],  allowedHeaders: ['Content-Type', 'Authorization'],}));// â”€â”€â”€ Rate Limiting â”€â”€â”€const apiLimiter = rateLimit({  windowMs: 15 * 60 * 1000, // 15 minutes  max: 100, // 100 requÃªtes par fenÃªtre  message: { error: 'Trop de requÃªtes, rÃ©essayez dans 15 minutes' },});const authLimiter = rateLimit({  windowMs: 15 * 60 * 1000,  max: 10, // 10 tentatives de login par 15 min  message: { error: 'Trop de tentatives de connexion' },});// â”€â”€â”€ Logging â”€â”€â”€app.use(morgan('dev'));// â”€â”€â”€ Body parsing â”€â”€â”€// IMPORTANT: Le webhook Stripe a besoin du body brut, donc on l'exclut du JSON parserapp.use((req, res, next) => {  if (req.originalUrl === '/api/orders/webhook') {    next(); // Le webhook gÃ¨re son propre parsing  } else {    express.json({ limit: '10mb' })(req, res, next);  }});app.use(express.urlencoded({ extended: true, limit: '10mb' }));// â”€â”€â”€ Servir les fichiers uploadÃ©s (images uniquement â€” les PDFs passent par les tokens) â”€â”€â”€const UPLOAD_DIR = path.resolve(process.env.UPLOAD_DIR || './uploads');app.use('/api/uploads/images', express.static(path.join(UPLOAD_DIR, 'images'), {  maxAge: '7d',  etag: true,}));// â”€â”€â”€ Routes API â”€â”€â”€app.use('/api/auth', authLimiter, authRoutes);app.use('/api/products', apiLimiter, productRoutes);app.use('/api/orders', orderRoutes); // Pas de rate limit sur le webhookapp.use('/api/download', downloadRoutes);app.use('/api/reviews', apiLimiter, reviewRoutes);app.use('/api/newsletter', apiLimiter, newsletterRoutes);// â”€â”€â”€ Routes SEO (sitemap, robots.txt, meta) â€” AVANT le SPA fallback â”€â”€â”€app.use('/', seoRoutes);app.use('/api/seo', seoRoutes);// â”€â”€â”€ Servir le frontend buildÃ© (production) â”€â”€â”€const frontendDist = '/app/frontend/dist';app.use(express.static(frontendDist));// SPA fallback â€” toutes les routes non-API renvoient index.htmlapp.get('*', (req, res) => {  if (!req.path.startsWith('/api/')) {    res.sendFile(path.join(frontendDist, 'index.html'));  } else {    res.status(404).json({ error: 'Route API non trouvÃ©e' });  }});// â”€â”€â”€ Gestion d'erreurs globale â”€â”€â”€app.use((err, req, res, next) => {  console.error('âŒ Erreur non gÃ©rÃ©e:', err);  if (err.code === 'LIMIT_FILE_SIZE') {    return res.status(413).json({ error: 'Fichier trop volumineux (max 50 Mo)' });  }  if (err.message?.includes('Seuls les fichiers PDF')) {    return res.status(400).json({ error: err.message });  }  if (err.message?.includes('Format d\'image')) {    return res.status(400).json({ error: err.message });  }  res.status(500).json({ error: 'Erreur interne du serveur' });});// â”€â”€â”€ DÃ©marrage â”€â”€â”€app.listen(PORT, '0.0.0.0', () => {  console.log(`â•”â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•—â•‘  ðŸŽ¨ ColoriMagiques API                  â•‘â•‘  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€   â•‘â•‘  Port     : ${PORT}                          â•‘â•‘  Mode     : ${process.env.NODE_ENV || 'development'}               â•‘â•‘  Frontend : ${FRONTEND_URL}    â•‘â•šâ•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•â•  `);});export default app;